# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status: documentation baseline complete; P0 implementation underway (gated)

This repository's documentation baseline — two authoritative source documents plus twelve documents derived from them, described below — is complete and frozen. A real implementation has since begun under `control_plane/` (a Java/Spring Boot Maven project), built one atomic, human-approved step at a time under the strictly gated protocol in `docs/execution-plan.md` §18.11 — see "Implementation status: `control_plane/`" further down for what actually exists and the rules for touching it. Everything in this section and the next few describes the planning corpus that governs that implementation, not the implementation itself:

| File | Role |
|---|---|
| `docs/Ent_Agent_LLM_Inference_Opt_Control_Plane_problemstatement.txt` | **The primary authoritative source** (EAIOC-SPEC-001). If any other doc conflicts with this file, this file wins. |
| `docs/Ent_Agent_LLM_Inference_Opt_Control_Plane_Engineering_Spec.md` (Rev 1.4) | Companion engineering specification — authoritative alongside the problem statement for downstream documents (e.g. the scenario matrix, see below), but subordinate to it if the two conflict. |
| `docs/architecture.md` (EAIOC-ARCH-001, Rev 1.3) | Full architecture: objectives, pipeline stages, component specs, optimization catalog. Status: PRE-IMPLEMENTATION, pending approval. |
| `docs/interfaces.md` (EAIOC-INTF-001, v1.2.0) | Contracts/schemas for all 71 interfaces (INTF-001–071) and 230+ types crossing component boundaries. |
| `docs/conventions.md` (EAIOC-CONV-001, Rev 1.1.0) | Mandatory engineering conventions (naming, module layout, fail-open/closed rules, anti-patterns) that any implementation must follow. |
| `docs/edge-cases.md` (EAIOC-EDGE-001, v1.2.0) | Catalogue of edge cases (EC-001–EC-213), each with detection/expected-behavior/fallback/observability/test requirements. |

These six documents are the stabilized baseline the rest of the documentation set builds on. `docs/scenario-matrix.md` is the next document in the chain: a populated scenario cross-validation matrix (263 scenarios across 30 domains, 93 compound) generated from `docs/prompts/generate-scenario-matrix.prompt.md` and reconciled against the baseline above — it has passed its own readiness check (verdict: READY FOR DOCUMENTATION BASELINE FREEZE; current edge-case reconciliation is 177 DIRECT / 35 PARTIAL / 0 NOT COVERED / 1 DUPLICATE, `CONTRA-001` resolved) and must pass it again before any later optimization-specific document is generated.

Twelve such documents now exist, generated in this order, each from its own prompt in `docs/prompts/` and each ending in its own `## <Name> Readiness` verdict (`READY FOR NEXT DOCUMENTATION PHASE`) that gates the next one:

| Document | Prompt | Adds |
|---|---|---|
| `docs/optimization-catalog.md` | `generate-optimization-catalog.prompt.md` | 20 `TECH-NNN` canonical techniques, 25 `DA-NNN` developer-agent modules (elaborated at schema/applicability level), 11 `P5 Index` inference-serving entries |
| `docs/provider-matrix.md` | `generate-provider-matrix.prompt.md` | Provider/model capability & accessibility matrix (`PROV-NNN`), the six-state Provider Accessibility Model (Discovered/Accessible/Available/Authorized/Recommended/Eligible), FTR tier assignments (evidence-labeled, never asserted as authoritative without source support) |
| `docs/cache-strategy.md` | `generate-cache-strategy.prompt.md` | Concrete key/TTL/economics policy for the seven canonical cache types (`CACHE-001`–`007`: EXACT/SEMANTIC/TOOL_RESULT/EMBEDDING/PROVIDER_NATIVE/APPLICATION/CONTEXT) |
| `docs/agent-optimization.md` | `generate-agent-optimization.prompt.md` | Full algorithms for `AL-001`–`006`/`AR-001`–`004` (agent loop, sub-agent economics, adaptive control) and the seven agent-loop-facing `DA-NNN` modules, deferred here by `optimization-catalog.md`'s own scope boundary |
| `docs/inference-optimization.md` | `generate-inference-optimization.prompt.md` | Full Control-Plane elaboration of the 11 P5/Layer-3 capabilities (`INFOPT-001`–`011`: prefix/KV-cache reuse, KV-cache compression, continuous batching, request scheduling, prefill/decode optimization, speculative decoding, quantization, model replica selection, GPU utilization, inference queue management) — strictly detection/routing/negotiation/measurement only, never infrastructure implementation (H17/H18); includes Control-Plane decision algorithms and capability/net-value/validation-coverage/traceability matrices |
| `docs/quality-gates.md` | `generate-quality-gates.prompt.md` | Concrete thresholds/methodology for the 10 `QG-NNN` quality dimensions (Accuracy/Task completion/Retrieval quality/Factuality/Schema compliance/Semantic equivalence/Safety/User satisfaction/Latency/Cost), verifier calibration procedure (VCL), compression-contract test suites (QO-001), and the mandatory per-optimization validation invariant (every optimization application gets its own quality checkpoint before admission to the next stage — batch-after-all validation is explicitly prohibited) |
| `docs/security.md` | `generate-security.prompt.md` | Concrete mechanisms for the 5 `GSP-NNN` Governance/Safety Plane components (SGE/DGE/TMG/HAG/CIS), narrowing (not closing — no local benchmark evidence exists) `SOURCE-GAP-ARCH-02`'s CIS/TMG mechanism gap; the fail-open-on-optimization/fail-closed-on-security distinction elaborated with 12 Control-Plane algorithms (A–L) and 8 security matrices, including the AuditRecord-immutability/SEC-013-deletion-propagation reconciliation |
| `docs/observability.md` | `generate-observability.prompt.md` | Telemetry pipeline architecture, metric-to-dashboard mapping, and alerting/SLO implementation for the 3 `OBS-NNN` dashboard tiers (`architecture.md` §31.1 order: Executive/Engineering/Product-Operations), the audit/explanation retrieval layer for the six §47.16 governed-decision categories (H15/NFR-010), and the observability failure/fallback matrix isolating the one deliberate fail-closed exception (a required audit-write failure, `EC-198`) from the chain's otherwise fail-open-for-optimization model; uses `scenario-matrix.md` Domain AC (`SCN-AUDIT-001`–`004`) as its dedicated scenario domain — no domain literally named `SCN-OBS-*` exists |
| `docs/eval.md` | `generate-eval.prompt.md` | Full algorithm-level elaboration of the 5 `EL-NNN` Optimization Experimentation and Learning modules (`architecture.md` §21: Shadow Optimization/Controlled Rollout/A-B Testing/Continuous Policy Learning/Regression Detector), plus the benchmarking-corpus, statistical-methodology (sample size/confidence/significance — all `PROPOSED DEFAULT — VALIDATE LOCALLY`), rollout-phase (§34), and regression-testing (§35, `EC-071`/`EC-072`) detail every other downstream document explicitly deferred here; narrows (does not close) `SOURCE-GAP-QUALGATE-01`/`02`; flags `interfaces.md` §18's still-undefined `RegressionReport` type as `SOURCE-GAP-EVAL-01`; no scenario domain among all 30 is dedicated to evaluation/benchmarking — uses verified Domain AD compound scenarios (`SCN-CMP-051/052/056/057`) instead |
| `docs/SCALING.md` | `generate-SCALING.prompt.md` | Capacity planning, throughput targets, and horizontal-scaling triggers beneath `architecture.md` §33 (Enterprise Deployment Model)/§37 (`NFR-008`/`NFR-014`), citing (not redefining) `SPC` (§47.4.1) and `XEC` (§47.3.2); since no pre-existing capacity catalog exists upstream, introduces its own 8-entry `SC-NNN` series (Control-Plane self-protection/cross-execution coordination/spend governance/cache/provider-model/agent fan-out/evaluation-benchmark/observability capacity), explicitly tagged `SOURCE-DERIVED` rather than presented as upstream-defined; records that Domain AA's concurrency scenarios (`SCN-CONC-001`–`006`) validate correctness only, not throughput/scale-out, as an honest gap rather than overclaimed coverage |
| `docs/implementation-plan.md` | `generate-implementation-plan.prompt.md` | Build-sequencing elaboration of `architecture.md` §36/`conventions.md` §25's six Implementation Priority tiers (P0–P5) — capability-level lifecycle decomposition (Contract & Design → Minimal Implementation → Integration → Observability → Security/Governance → Quality Validation → Scenario Validation → Failure/Recovery → Capability Gate, §18) for 6 full exemplar capabilities plus 1 P5 boundary/sequencing treatment, with compact sequencing/dependency records for the rest; resolves `optimization-catalog.md`'s `SOURCE-GAP-OPTCAT-01` by dispositioning all 25 `DA-NNN` modules into the P0–P5 tiers (20 P1-equivalent/3 P2/1 P3/1 P4), each tagged `PROPOSED DEFAULT — VALIDATE LOCALLY`; keeps Implementation Priority (P0–P5), Maturity Level (0–5), and `scenario-matrix.md`'s unrelated P0–P3 test-priority scale explicitly distinct; records that no scenario or edge case validates build sequencing itself as an honest, non-blocking gap |
| `docs/requirements-traceability.md` | `generate-requirements-traceability.prompt.md` | The canonical cross-document forward-traceability layer that no prior document could produce (each was generated before its siblings existed): all 138 `OBJ`/`SEC`/`NFR`/`H`/`AC` requirement IDs individually traced to their owning downstream document(s) or honestly marked `NO DOWNSTREAM ELABORATION` (23 of them); a master source-gap register of 50 distinct semantic gaps consolidated across the entire corpus, exposing cross-document narrowing/resolution relationships (e.g. `security.md` narrowing half of `SOURCE-GAP-ARCH-02` while `provider-matrix.md` leaves the other half open) that no single document's own register could show; a master contradiction register (4 IDs, all resolved); explicit ADR-candidate-vs-`BLOCKED BY ADR` distinction |

Every one of these twelve downstream documents treats the others as authoritative-alongside the original seven — subordinate to them if either conflicts, per each document's own `## SOURCE CONTRADICTIONS` section.

**The originally-planned documentation chain is complete.** `docs/adr/` holds its final artifact: 7 files (`0000-index.md` + `0001`–`0006`) generated from `docs/prompts/generate-adr-set.prompt.md`, recording the six implementation-blocking technology/pattern questions `implementation-plan.md` §25 identified but deliberately did not decide (cache/KV backing store, execution-truth/versioned-state store, CI/CD platform, telemetry backend, provider-adapter implementation pattern, `XEC` coordination/locking mechanism). Every ADR's `Status` is `PROPOSED`, never `ACCEPTED` — this repository has zero local benchmark or production evidence, so no ADR here selects an actual technology; each instead records the decision's shape (context, real cited constraints, generically-described candidate options) for a future team with actual evaluation evidence to accept quickly. `requirements-traceability.md`'s `BLOCKED BY ADR = 0` is independently re-confirmed in the index — an ADR candidate is a decision that should eventually be recorded, not a decision implementation is currently stalled on. The index's own formal dependency graph found **0 hard dependencies** among the six. The index ends with `ADR SET COMPLETE — READY FOR IMPLEMENTATION READINESS GATE`, not the `READY FOR NEXT DOCUMENTATION PHASE` phrase every chain document used, because it named a genuinely new checkpoint the original chain never planned for.

**One further artifact now exists beyond the original chain: `docs/implementation-readiness-gate.md`**, generated from `docs/prompts/generate-implementation-readiness-gate.prompt.md`. The ADR index's own closing language asked a question none of the twelve chain documents or the ADR set actually answered — "is the entire EAIOC system ready to be coded?" — and this document answers it by independently *re-verifying* other documents' summary claims rather than citing them at face value (the first artifact in the repository to do so): it re-derives the requirement/coverage counts, the source-gap and contradiction totals, and — the check nothing upstream performed — whether each `PROPOSED` ADR touching a P0/P1 item actually has a stated interim implementation path. That re-verification surfaced two genuine, non-blocking findings recorded as `SOURCE-GAP-IRG-01`/`02` (`ADR-0004`'s telemetry backend has no documented interim sink, unlike `ADR-0001`'s explicit in-memory-first path; the ADR index's claim that its `ADR-0002`↔`ADR-0006` shared open question is "recorded identically in both ADRs" is itself inaccurate — only `ADR-0006` states it). Its verdict, `IMPLEMENTATION READINESS GATE: PASS WITH CONDITIONS — P0 MAY BEGIN; OBSERVABILITY HAS A CAPABILITY-LEVEL CONDITION`, is deliberately scoped: the condition affects only the Observability P0 item's path to a fully-closed Minimal Implementation, not the other 7 of 8 P0 items, which are unconditionally `READY`. As with the ADR set, this completion describes the documentation/governance layer only — the system itself remains `PRE-IMPLEMENTATION — Level 0 (RESEARCH)`, and this document does not accept any ADR, select any technology, or authorize writing code.

**A second further artifact now exists beyond the original chain and beyond the IRG: `docs/execution-plan.md`** (`EAIOC-EXECPLAN-001`, currently **v1.0.5**), generated from `docs/prompts/execute-p0-foundation.prompt.md` — a user-authored "master prompt" that replaced an earlier Claude-drafted V1 at the same path, per this repo's established pipeline cycle (see the Prompt-writing rule section below). Unlike every other generated document, this prompt defines two modes: **Mode A** (plan generation — what produced `execution-plan.md`; source code may not be written in this mode) and **Mode B** (gated implementation execution — triggered separately, later, by an explicit `Execute EXE-P0.<n>.<letter>` command per `execution-plan.md` §18.11's own command protocol; since invoked — see "Implementation status" below). `execution-plan.md` is the operational bridge between `implementation-plan.md`'s build-sequencing logic and an actual coding session: it validates a brainstormed technology baseline (`docs/scratch/EAIOC_Tech_Stack_Brainstorming_Conversation.txt` — Java/Spring Boot for the P0 request-critical core, Python/FastAPI and Go deferred, Postgres/Redis/Kafka/Kubernetes classified as enterprise-target-not-P0, OpenTelemetry gated on `ADR-0004`) against the frozen architecture (zero conflicts found — the architecture is deliberately language-neutral) and decomposes the six unconditionally-`READY` P0 capabilities into **48 atomic `EXE-P0.<n>.<letter>` execution units** (8 lifecycle sub-phases A–H × 6 capabilities) **plus 6 Capability Gates — 54 total gated checkpoints** (the Gates are review/promotion checkpoints, never atomic units, never given their own commit or `EXE-P0` ID — see below). It records three new non-blocking gaps (`SOURCE-GAP-EXECPLAN-01`–`03`).

`execution-plan.md` has since been through five **surgical-correction passes**, each from its own versioned prompt at `docs/prompts/Execution_Plan_Surgical_Correction_Prompt_v1.0.<N>.md` (a further extension of the pipeline pattern — see the Prompt-writing rule section below): **v1.0.1** removed a Sub-phase A/B shared-commit contradiction and corrected wording that could be misread as permitting parallel *execution* of independent capabilities in Mode B (dependency independence ≠ execution parallelism — Mode B is always strictly sequential); **v1.0.2** reclassified the Capability Gate as a post-lifecycle review/promotion checkpoint rather than a 9th implementation sub-phase, fixing every dependent count reference from "54 atomic units" to the 48+6=54 split stated above; **v1.0.3** added `§18.11 Claude Code Mode B Execution Command Protocol` — the canonical `Execute EXE-P0.<n>.<letter>` / `Approve EXE-P0.<n>.<letter>` / `Approve CAPABILITY-GATE P0.<n>` command syntax, the required per-unit response format, a command-state table, and a strong no-automatic-chaining rule (no passing test, clean commit, or closed gate ever itself authorizes the next unit — only an explicit user command does); **v1.0.4** (triggered by `EXE-P0.2.B` stopping on a missing prerequisite) assigned ownership of the shared `core/interfaces`/`core/schemas`/`core/errors` foundation to `EXE-P0.1.A` (§18.13), added post-hoc reconciliation of already-executed units plus the remediation units `REM-P0.1.A-01`/`-02` that close the resulting gap without rewriting history (§18.14), a hard pre-execution guard on `EXE-P0.2.B` (§18.15), and a mandatory **two-stage checkpoint** — Claude Code's own AI Verification (§18.12) before every human approval, including an AI Gate Verification before every Capability Gate approval — plus N/A-sub-phase and out-of-band-commit rules (§18.16) and an execution state model (§18.17); **v1.0.5** was consistency-only: only A–H are lifecycle sub-phases (the Gate is separate), the capability-level `Execute P0.<n>` shorthand is retired (the canonical `Execute EXE-P0.<n>.<letter>` is mandatory, §18.11.2), and the readiness conclusion now says `PLAN READY: YES` but `MODE B READY for EXE-P0.2.B: NO` while `SOURCE-GAP-EXECPLAN-04` is open. Each pass's own terminal verdict (e.g. `FINAL EXECUTION PLAN v1.0.4 CORRECTION COMPLETE — READY FOR FREEZE`) supersedes, rather than duplicates, the prior version's line as the document's true final line — the same discipline every other chain artifact's terminal verdict follows. `docs/execution-plan-p0-steps.md` is a companion **human/operator runbook** mirroring the plan's canonical commands in copy-paste-ready form for actually running Mode B one checkpoint at a time; its §20 records the current position and the remaining command sequence. It explicitly does not replace `execution-plan.md` (the plan wins on any conflict) — never reconcile the plan against it — but it is kept in sync with the plan on every correction pass (see the Prompt-writing rule section below).

As with the ADR set and the IRG, `execution-plan.md` itself remains planning output only — no `control_plane/` source tree existed at the time this document was generated, and this document alone authorizes no code, accepts no ADR, and finally selects no technology. Mode B has since begun separately, under its own explicit per-unit approval gate (see "Implementation status" below for what that has actually produced) — the plan's own maturity claim about itself is unchanged even though the tree it describes now exists.

`docs/strategy/control-plane-stress-test.md` (+ its `.html` copy, EAIOC-STRAT-001) is a separate, explicitly **non-authoritative** brainstorming/critique document (architecture challenge, research landscape, competitive analysis) written before the baseline freeze. It never overrides the six documents above where they disagree with it.

Three further files live in `docs/scratch/`, outside the documentation chain entirely: `EAIOC_Presentation.html` (a stakeholder-facing summary deck of the architecture), `EAIOC_Tech_Stack_Brainstorming_Conversation.txt` (a raw brainstorming transcript exploring implementation technology choices), and `eaioc-abbreviations.html` (a self-contained mobile-friendly abbreviation/ID-series reference and quiz tool, verified against the full doc set). Like the stress-test document, treat all three as non-authoritative scratch/presentation/reference material — never a source to reconcile documentation against, and never a substitute for the ADRs in `docs/adr/` when an actual technology question comes up.

### Dynamic Execution / Control-Plane hardening amendment (2026-09-10)

The baseline was hardened once, after initial stabilization, to make dynamic/runtime execution semantics explicit (they were previously implicit in a request/response-shaped model). This added 13 components and 13 interfaces without touching or renumbering anything that existed before:

- **What was added:** Execution State Manager (ESM), Context Version Manager (CVM), Workflow Version Manager (WVM), Checkpoint Manager (CPM), Reconciliation Engine (RE), Context Integrity Gate (CIG), Context Expansion Controller (CEC), Permission Revalidation (PRV), Dynamic Policy Evaluation (DPE), Capability/Availability Resolver (CAR), Stale Result Protection (SRP), Supersession Manager (SPM), Recovery Coordinator (RCO).
- **Where it lives:** `architecture.md` §46 (component specs, state machine, integration points), `interfaces.md` §42 / INTF-050–062, Engineering Spec §41 (new objectives OBJ-015–022), `conventions.md` (the `execution/` package, §2.2 dependency rules, §16.2 fail-open/closed table), `edge-cases.md` §43 / EC-080–140, and reconciled into `scenario-matrix.md`.
- **Central invariant this amendment exists to enforce:** these components own *execution truth* over time (a request is a versioned, resumable, reconcilable lifecycle, not a single atomic transaction) — see the pattern below.

### Operating Model, Governance, and Trust Boundaries hardening amendment (2026-09-15, reconciled 2026-09-16)

A second hardening pass, layered on top of the first, translates PS §52's twenty hardening requirements (H01–H20) into nine new/extended components spanning four planes — Governance/Safety, Execution Truth, Optimization Intelligence, and Execution Adapter. Purely additive: it does not replace or renumber anything in §1–46.

- **What was added:** Spend Governance Engine (SGE — tenant/org/user/app spend limits and runaway-cost circuit-breaking), Data Governance Engine (DGE — content-sensitivity classification and deletion propagation across every persistent surface), Tool/MCP Trust Gate (TMG — tool/MCP identity, schema-staleness, and authorization checks independent of ROI), Human Approval Gate (HAG — policy-designated approval before consequential/irreversible actions), Content Integrity Screen (CIS — prompt-injection/content-integrity screening for all non-user-input content before any optimization stage admits it), Feasibility Tier Registry (FTR), Cross-Execution Coordinator (XEC), Self-Protection Controller (SPC), Verifier Calibration Layer (VCL).
- **Where it lives:** `architecture.md` §47 (component specs, plane taxonomy, §47.1 overview table), `interfaces.md` §43 / INTF-063–071, Engineering Spec §42 (new objectives OBJ-023–035, SEC-011–016, NFR-014, AC-039–053), `conventions.md` (new `governance/` package for SGE/DGE/TMG/HAG/CIS, `execution/cross_execution/` for XEC, `intelligence/self_protection/` for SPC, `intelligence/verifier_calibration/` for VCL, `providers/feasibility_tier/` for FTR — see §7, §12, §13, §16, §22, §23, §24, §27), `edge-cases.md` §44 / EC-141–213, and reconciled into `scenario-matrix.md`.
- **Central invariant this amendment exists to enforce:** governance/safety decisions (spend, data sensitivity, tool trust, human approval, content integrity) are independent of and never inferred from optimization/ROI/cost signals — e.g. `WITHIN_BUDGET` never implies authorized, and cost-efficiency never substitutes for an authorization check.
- A third pass, `architecture.md` §48 (2026-09-16, "Final Foundational Document Reconciliation"), then closed remaining stale cross-references and one source gap (`SOURCE-GAP-ARCH-03`) across the baseline without adding new components — it's reconciliation, not new scope.

Because `docs/` itself was written before any implementation existed:
- **Do not assume a language or framework applies outside `control_plane/`.** `conventions.md` still describes its package layout as a language-neutral pseudo-structure (e.g. `control_plane/core/interfaces/`) — nothing in `docs/` should be edited to assume Java/Spring Boot just because `control_plane/` now happens to use them for its own P0 slice.
- **Do not invent build, lint, or test commands anywhere outside `control_plane/`.** Inside it, real commands now exist — see "Implementation status" below; don't extrapolate them to the rest of the repo or invent ones `control_plane/pom.xml` doesn't actually support.
- The first task in any implementation work is almost always to reconcile new code against `docs/`, not to design from scratch — and, as of this baseline, to follow the gated `EXE-P0.<n>.<letter>` protocol below rather than writing code freely.

## Implementation status: `control_plane/` (Java 25 / Spring Boot 4.1.x, P0 Capability 1 lifecycle done pending remediation + Gate, Capability 2 blocked at B)

`control_plane/` is a single-module Maven project (`groupId com.eaioc`, `artifactId control-plane` — both **proposed**, not specified anywhere upstream, flagged as such in the commit that introduced `pom.xml`). Technology baseline validated against the frozen architecture with zero conflicts in `docs/execution-plan.md` §6, and confirmed resolvable in this environment by `docs/implementation-env-readiness-check-p0.md`.

**Build and test** (run from `control_plane/`, which has its own `pom.xml` — the repo root has none):
```
mvn test                                    # full test suite
mvn test -Dtest=CostLedgerStoreTest         # one test class
mvn test -Dtest=CostLedgerStoreTest#writeThenReadBack_roundTripsCorrectly   # one test method
mvn compile                                 # compile only
```
No linter or static-analysis tool is configured — don't invent one. `control_plane/target/` is Maven build output, gitignored, never committed.

**What exists right now** (check `git log --oneline` and the runbook's §20 for the live state — each executed unit that produces changes is exactly one commit titled `P0-<n>.<letter>: <Capability> — <Sub-phase>`; remediation units use `P0-1.A-REM-0N: …`; commit `c3d6ecf` is an out-of-band docs/editor-settings commit and never evidence of a completed unit):
- **Capability 1 — Token Accounting and Cost Ledger:** Sub-phases A, B, C, D, E, G, H committed. There is intentionally **no F commit**: every P0 capability's Sub-phase F (Quality Validation) is `NOT APPLICABLE` per `execution-plan.md` §18.4–18.9 — `QG-NNN` dimensions gate optimization techniques, and P0 capabilities are substrate, not techniques. Whether `CAPABILITY-GATE P0.1` was formally approved is not recorded in the repo; ask rather than assume. Code: `CostLedgerEntry` (incl. `unverifiedFallback()` — the rule-5 recovery path: zeroed fields, `verified=false`, never dropped or guessed)/`CostLedgerStore` (append-only; duplicate `entryId` rejected)/`LedgerObservability`/`MissingTenantIdException`/`DuplicateLedgerEntryException` under `src/main/java/com/eaioc/controlplane/accounting/ledger/`, with one test class per sub-phase (`CostLedgerStoreTest`, `…IntegrationTest`, `LedgerObservabilityTest`, `…TenantIsolationTest`, `…ScenarioValidationTest`, `…FailureRecoveryTest`) plus `StubPipelineStageCaller`. Design note: `control_plane/accounting/ledger/LedgerEntry.md` (resolves a real schema discrepancy between `architecture.md` §27.1's ledger field catalog and `interfaces.md` `INTF-047 CostLedgerEntry`, in favor of the latter).
- **Capability 2 — Baseline Benchmark Harness + Quality Evaluation:** only Sub-phase A (design note `control_plane/evaluation/EvaluationFramework.md`, citing `INTF-030` `EvaluationFramework`/`EvaluationRun`/`EvaluationComparison`; `RegressionReport` is out of scope as `SOURCE-GAP-EVAL-01`). No Java code for it yet. `EXE-P0.2.B` stopped because `INTF-030`'s `run_baseline(request: ControlPlaneRequest)` has no `ControlPlaneRequest` type: **no `core/` package exists yet**.
- **Shared `core/` foundation — the current blocker (`SOURCE-GAP-EXECPLAN-04`):** owned by `EXE-P0.1.A` since plan v1.0.4, but the historical `EXE-P0.1.A` (`a345742`) deferred it and no later unit built it (reconciled `GAP FOUND`, `execution-plan.md` §18.14.1). It is created only through `REM-P0.1.A-01` (design note `control_plane/core/CoreFoundation.md`: every `ControlPlaneRequest`/`OptimizationPlan`/`ControlPlaneError` field and sub-type taken verbatim from live `interfaces.md` §2.1/§3.2/§25, each marked realized-now or deferred) and then `REM-P0.1.A-02` (Java types under `com.eaioc.controlplane.core.{interfaces,schemas,errors}`, importing nothing else from `control_plane`). After that come Capability 1's Gate review and then `EXE-P0.2.B`, which has a hard pre-execution guard (§18.15). **A consumer unit must never create `core/` types itself, and must never use a harness-local stand-in type** — if they're missing, report `AI VERIFICATION: BLOCKED`.

Sub-phase A design notes live as `.md` files beside the future package path under `control_plane/<area>/`, not under `src/`. Package root `com.eaioc.controlplane` is one concrete Java realization of `conventions.md` §2.1's conceptual tree (e.g. its `accounting/` leaf → `com.eaioc.controlplane.accounting.ledger`), not a literal reproduction of that pseudo-structure's directory names.

**Don't trust `execution-plan.md` §18.4–18.9 rows blindly.** Prior units re-verified row claims against the live docs and found errors (e.g. EXE-P0.1.G's row said "NO DEDICATED SCENARIO", but `SCN-TEN-003` and `SCN-AUDIT-003` directly apply). Re-check a row's cited IDs against `scenario-matrix.md`/`edge-cases.md` before executing, and note any correction in the commit message.

**The one rule that matters most for any future session touching this code:** this implementation is being built under `docs/execution-plan.md`'s strictly gated Mode B protocol (§18.11–§18.18, v1.0.5), one atomic unit at a time. **Never write, extend, or "helpfully continue" any `EXE-P0.*` or `REM-*` work without an explicit command naming that unit from the user in that session.** Commands: `Execute EXE-P0.<n>.<letter>` / `Execute REM-P0.1.A-0N`; `Approve …` with the same ID; `Review CAPABILITY-GATE P0.<n>` (review only); `Approve CAPABILITY-GATE P0.<n>`. Always use the full unit ID — `Execute P0.<n>` and vague phrasings like "continue with P0" are not valid commands; ask for the canonical ID instead (§18.11.2, §18.11.9). For each unit:
1. Re-read the live sources, not just the plan row.
2. Check that prerequisites actually exist and were approved.
3. Do only that unit's work and run its tests, including the failure-path test.
4. Produce the §18.12.3 **AI VERIFICATION REPORT**: nine dimensions (scope, source, requirement, architecture, tests, failure path, git, prerequisites, source gaps) and exactly one verdict — `PASS`, `BLOCKED`, `NOT APPLICABLE`, or `PASS WITH DOCUMENTED NON-BLOCKING GAP`.
5. Commit only on a pass verdict. An N/A unit gets no commit, never a fake one. A commit holds only the unit's own files — never `CLAUDE.md`, `.vscode/`, or status docs.
6. End with `### AI VERIFIED — AWAITING HUMAN APPROVAL` then stop. On `BLOCKED`, end with `### AI VERIFICATION BLOCKED` instead: no commit and **no approval request**.

The Capability Gate works the same way: AI Gate Verification (`AI CAPABILITY GATE VERIFICATION: PASS / BLOCKED`) first, then the human `Approve CAPABILITY-GATE P0.<n>`, which is valid only after an AI PASS. Your own AI PASS, a passing test suite, a clean `git status`, a commit, or a closed Gate never authorize the next unit — only an explicit user command does (§18.11.12, §18.12.5). Before executing any unit, read `docs/execution-plan.md`:
- §18.4–18.9: the exact unit definitions — which fields, files and tests each sub-phase covers.
- §18.11–18.18: the full command, verification and reporting protocol.

## What this project is

An **Enterprise Agent & LLM Inference Optimization Control Plane**: a policy-driven optimization/governance layer that sits between AI applications/agents and LLM providers (and optionally the inference-serving layer). It is not a prompt-compression utility — its job is to decide, for every request, the cheapest safe way to satisfy the task, up to and including deciding that **no LLM call is needed at all** (cache hit, programmatic result, early exit).

Central objective (from `architecture.md` §4, restated verbatim in `conventions.md` §1.1):

```
MINIMIZE: Total Safe Inference Cost
SUBJECT TO: Quality, Correctness, Security, Reliability, Latency, Freshness, Compliance, Task completion
```

Token reduction alone is never success. Every optimization must pass a quality gate and net-positive cost accounting (`Net Savings = Inference Cost Avoided - Optimization Compute Cost - ...`) before being counted or enabled.

Four first-class execution environments must all be supported by one common optimization core (not separate optimizers per surface): (A) generic LLM apps, (B) autonomous/tool-using agents, (C) developer/coding agents (Cursor, Claude Code, Copilot, etc. — first-class, not optional), (D) multi-agent/sub-agent systems.

## Architecture at a glance

Three optimization layers governed by a shared **Optimization Intelligence Layer (OI)**:

- **Layer 1 — Agent/Workflow Optimization**: task planning, tool selection/execution, sub-agent economics, agent loops, early exit.
- **Layer 2 — Context Optimization**: retrieval, ranking, pruning, dedup, compression, reordering, budgeting, caching, freshness, dependency graphs.
- **Layer 3 — Inference/Serving Optimization** (optional, P5): model routing/cascade, reasoning budget, output budget, batching, KV cache, speculative decoding, quantization. Must stay measurement-separate from Layers 1/2.
- **OI layer** (OI-001..005) decides per-request which stages run, whether the optimizer's own overhead is worth it, how deep to optimize, context item ROI, and outcome-based cost.

The full per-request pipeline (T0 → T3) is documented stage-by-stage in `architecture.md` §8 (generic) and §9 (developer-agent variant, which runs on *every* model-bound iteration, not just the first request). The canonical 24-stage default execution order is in `conventions.md` §6.1 — read it before proposing a different stage ordering; reordering is allowed only with benchmark evidence.

Component IDs follow `<LAYER>.<SEQUENCE>-<NAME>` (e.g. `T1.3-CONTEXT-PRUNER`, `OI.1-DECISION-ENGINE`, `DA.008-TOOL-SCHEMA-OPTIMIZER`) — see `conventions.md` §3.1. Its proposed package layout is meant to mirror these IDs under `control_plane/`; a first, partial Java realization of that layout now exists — see "Implementation status" below — but most of the tree remains unscaffolded pseudo-structure. Check `conventions.md` §2.1 before creating any new module so its location matches this map.

## Non-negotiable rules for any implementation work here

These are structural, not stylistic — violating them is a spec violation, not a style nit:

1. **Provider neutrality.** Core code must never import or hard-code a specific LLM provider, model name, agent framework, MCP library, or embedding model. All of that goes through adapter interfaces (`providers/adapters/`); provider-specific data goes in an opaque `provider_hints` map, never a first-class schema field.
2. **Fail-open on optimization, fail-closed on security — and these are not the same category of failure.** An optimization stage (pruning, compression, caching, routing, reconciliation retries, ...) that fails must fall back to the unoptimized/original path and let the request continue, *provided the request remains authorized and policy-compliant*. A failure to establish or evaluate authorization/policy/PII-scrubbing itself — as opposed to an optimization stage failing on top of an already-established authorization — must reject the request. Never conflate the two: an innocent compressor bug on an authorized, policy-compliant request must never turn into a full request rejection (see `conventions.md` §16.2, `edge-cases.md` §43.13).
3. **Never reduce reasoning budget solely to hit a token target.** Reasoning cuts must be benchmarked against a quality baseline first (`conventions.md` §10.3).
4. **Tenant isolation is absolute.** Every cache key, memory entry, cost record, and event must be scoped by `tenant_id` as the first namespace component. Cross-tenant lookups, sharing, or cost blending are unconditionally prohibited (`conventions.md` §13.2).
5. **Never fabricate savings.** If token/cost accounting can't be verified, mark it `unverified` and exclude it from savings reporting — don't report a number you can't back.
6. **Security-classified content (SEC-protected instructions, PII policies, auth rules) is never pruned, compressed away, or deduplicated out**, regardless of relevance score or budget pressure.
7. **Research benchmark percentages are not production guarantees.** Any number quoted from a paper (e.g. LLMLingua, RouteLLM, FrugalGPT) in the docs is a validation target to re-measure locally, never a number to hard-code as an expected result.
8. **Full conversation-history replay to a sub-agent is prohibited.** Sub-agent handoffs must go through the compressed `SubAgentContextHandoff` (objective, findings, evidence, decisions, unresolved questions) — see `conventions.md` §12.4.
9. See `conventions.md` §24 for the full anti-pattern list (e.g. fixed Top-K for every query, always using the strongest/cheapest model, indiscriminate compression) — treat it as a code-review checklist.

## Working with these documents

- The problem statement (`.txt`) is authoritative; `architecture.md`, `interfaces.md`, and `conventions.md` are its derived, cross-referenced elaborations and must not silently drop, simplify, or reinterpret anything in it. If you're asked to change one of the derived docs, verify consistency against the problem statement rather than editing in isolation.
- Every requirement in these docs is traceable via IDs: `OBJ-NNN` (35 objectives — OBJ-001–014 original, OBJ-015–022 from the first hardening amendment, OBJ-023–035 from the second), `SEC-NNN` (16 security requirements — SEC-001–010 original, SEC-011–016 from the second hardening amendment), `NFR-NNN` (14 non-functional requirements), `AC-NNN` (53 acceptance criteria), `EC-NNN` (213 edge cases — EC-001–079 original, EC-080–140 from the first hardening amendment, EC-141–213 from the second), `DA-NNN` (25 developer-agent modules — schema/applicability in `optimization-catalog.md`, full algorithms for the agent-loop-facing subset in `agent-optimization.md`), `OI/CL/CE/TE/AL/AR/QO/EL-NNN` (intelligence/lifecycle/economics modules — `AL-NNN`/`AR-NNN` fully elaborated in `agent-optimization.md`), `SCN-<CODE>-NNN` (scenario matrix, 30 domain codes A–AD, 263 scenarios). The seven generated downstream documents add their own series on top of these: `TECH-NNN` (20 canonical optimization techniques) and `P5 Index` entries (11, index-level only) in `optimization-catalog.md`; `PROV-NNN` in `provider-matrix.md`; `CACHE-NNN` (exactly 7, one per canonical cache type) in `cache-strategy.md`; `INFOPT-NNN` (exactly 11, one per P5 capability, detection/routing/negotiation/measurement only) in `inference-optimization.md`; `QG-NNN` (exactly 10, one per `architecture.md` §28 quality dimension) in `quality-gates.md`; `GSP-NNN` (exactly 5, one per `architecture.md` §47.2 Governance/Safety Plane component: SGE/DGE/TMG/HAG/CIS) in `security.md`; `OBS-NNN` (exactly 3, one per `architecture.md` §31.1 dashboard tier: Executive/Engineering/Product-Operations) in `observability.md`; `EL-NNN` (exactly 5, one per `architecture.md` §21 Optimization Experimentation and Learning module) in `eval.md`; `SC-NNN` (exactly 8, one per independently-scaled capacity surface — `SCALING.md`'s own `SOURCE-DERIVED` synthesis, not an upstream-enumerated catalog) in `SCALING.md`; `ADR-NNNN` (exactly 6, under `docs/adr/`, each `Status: PROPOSED`) recording the technology/pattern decisions `implementation-plan.md` identified but did not make. When adding new material, give it an ID in the matching series and cross-reference it the way existing entries do, rather than inventing a new numbering scheme.
- These files are large (architecture.md ~4000 lines, interfaces.md ~4550 lines, conventions.md ~1700 lines, edge-cases.md ~8300 lines, scenario-matrix.md ~13,800 lines). Prefer `Grep` for a known section/ID over reading a whole file, and when reading, target the specific numbered section (table of contents at the top of each doc) rather than paging through sequentially. For `scenario-matrix.md` specifically, delegating a research/cross-reference pass to a forked subagent (rather than reading the whole file into your own context) is usually the right call.
- The edge-case doc format is fixed (Domain/Objective/Severity/Likelihood/Reversible fields + Scenario/Trigger/Why It Matters/Detection/Expected Behavior/Fallback/Safety Implications/Observability/Testing Requirements). Any new edge case you document must follow this exact structure so it stays consistent with the other entries.
- **Source gaps and contradictions are tracked, not resolved by inventing content.** When a document is genuinely underspecified by its sources, or two documents disagree, record it as `SOURCE-GAP-NNN` or `CONTRA-NNN` (see `scenario-matrix.md`'s own gap/contradiction sections, and the `> **Source Gap:**` callouts in `edge-cases.md` §43) rather than guessing at normative behavior. A gap only gets marked resolved when a source document is actually updated to close it — never by removing the gap record to make coverage numbers look better. Each of the seven generated downstream documents follows the same discipline with its own document-scoped ID prefix rather than renumbering an upstream gap it merely carries forward: `SOURCE-GAP-OPTCAT-NN`/`CONTRA-OPTCAT-NN` in `optimization-catalog.md`, `SOURCE-GAP-PROVMTX-NN` in `provider-matrix.md`, `SOURCE-GAP-CACHESTRAT-NN` in `cache-strategy.md`, `SOURCE-GAP-AGENTOPT-NN` in `agent-optimization.md`, `SOURCE-GAP-INFOPT-NN` in `inference-optimization.md`, `SOURCE-GAP-QUALGATE-NN` in `quality-gates.md`, `SOURCE-GAP-SECURITY-NN` in `security.md` (one of which explicitly narrows, but does not claim to close, `SOURCE-GAP-ARCH-02`), `SOURCE-GAP-OBS-NN` in `observability.md`, `SOURCE-GAP-EVAL-NN` in `eval.md` (one of which narrows, but does not close, `SOURCE-GAP-QUALGATE-01`/`02`), `SOURCE-GAP-SCALING-NN` in `SCALING.md`, `SOURCE-GAP-IMPL-NN` in `implementation-plan.md` (which also resolves-in-place, without editing the source document, `optimization-catalog.md`'s `SOURCE-GAP-OPTCAT-01`), `SOURCE-GAP-REQTRACE-NN` in `requirements-traceability.md` (which also consolidates every other prefix above into one cross-corpus master register rather than introducing a large number of new gaps of its own).
- **Reconciliation passes are single-file-scoped.** The established pattern in this repo (used for the `conventions.md`, `edge-cases.md`, and `scenario-matrix.md` hardening passes) is: read all the source documents needed to reconcile, but write to only the one named target file; verify afterward (via `git diff` if a git repo exists, or file-modification-time comparison otherwise) that no other file changed; and stop for the next instruction rather than cascading into the next document in the chain unprompted.

## Prompt-writing rule (existing project rule)

`.claude/rules/prompt-writing.md` applies whenever asked to create/write/draft/review a prompt for a fresh Claude/Claude Code session, for paths matching `outputs/*prompt*.md`, `outputs/**/prompt*.md`, or `prompts/**`. Key points: deliver the finished prompt only in chat inside one fenced code block (never write a file to those paths), then copy it to the clipboard via a quoted `pbcopy` heredoc as the last step. Target a >95/100 self-score against the rubric in that file before delivering. The rule carves out an exception for the `docs/prompts/generate-<name>.prompt.md` → `docs/<name>.md` documentation-generation pipeline (write the file, expect the user to paste in their own larger "consolidated master prompt" before execution, re-read it fresh from disk rather than trusting an earlier summary).

`docs/prompts/execute-p0-foundation.prompt.md` extends that same pipeline pattern one step further: it is a user-authored, two-mode "master prompt" (Mode A generates `docs/execution-plan.md`; Mode B is gated code execution — see the repository-status section above) rather than a single `generate-<name>.prompt.md` → `docs/<name>.md` document. Treat it the same way as the pipeline exception — write/edit it in place at that path rather than delivering it only in chat, and re-read it fresh from disk before executing either mode, since the user may replace its content between sessions exactly as they did for this one.

`docs/prompts/Execution_Plan_Surgical_Correction_Prompt_v1.0.<N>.md` is a further, sequential extension of the same pattern: each such file is a one-shot, single-purpose, user-authored correction prompt targeting `docs/execution-plan.md` alone (never a rewrite — the file's own rules are explicit about what to preserve unchanged), bumping the plan by exactly one patch version and ending in its own version-specific terminal verdict line (e.g. `FINAL EXECUTION PLAN v1.0.4 CORRECTION COMPLETE — READY FOR FREEZE`), appearing exactly once as the true final line. When the user points at one of these (a new `_v1.0.<N>.md` file will keep appearing as corrections continue):
- Re-read it fresh from disk and apply only what it specifies.
- Also bring the operator runbook `docs/execution-plan-p0-steps.md` in line with it. The user asked for this explicitly even though the prompts themselves restrict scope to `execution-plan.md`; note the deviation in the plan's own version self-check.
- Check that the prompt's own claims about repository state match `git log`. The v1.0.4 prompt omitted Capability 1's G commit; record the actual state, not the prompt's.
- Verify via `git status --short`/`git diff --stat` that only those two files changed.
- Do not commit unless asked. These correction passes accumulate uncommitted until the user is ready to commit them together.

## Custom subagent and skill (existing project assets)

`.claude/agents/eaioc-guide.md` defines an `eaioc-guide` subagent: a personal Q&A/research companion for doubts about this project (conceptual, architectural, or technology-choice questions) or for verifying an external claim, technology, or paper against the web or the project's own docs. It grounds itself in this file and the specific doc/section/ID a question touches before answering, flags genuine gaps as `SOURCE-GAP`/`CONTRA` rather than inventing an answer, is advisory-only (never edits the baseline documents itself), and always answers via a published HTML artifact rather than plain chat text. Claude Code routes matching questions to it automatically. It has no `Write` tool, so whichever agent performs the actual publish step builds the HTML.

`.claude/skills/eaioc-dashboard/SKILL.md` is a companion project skill invoked right after `eaioc-guide` (or whichever agent is publishing) grounds its findings and right before finalizing the report's page structure: it adds a small graphical dashboard (stat tiles, meters, small bar/donut charts) near the top of the published artifact whenever the answer's material has genuine quantitative content — ID-series counts, status/verdict distributions, coverage/reconciliation percentages, maturity levels — sourced only from figures the grounded answer already cites, never invented to fill a tile. It is skipped honestly (not treated as a gap) when a question is purely conceptual with nothing real to chart.
